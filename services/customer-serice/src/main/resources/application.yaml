server:
  port: 8082

spring:
  application:
    name: customer-service

  datasource:
    url: jdbc:mysql://localhost:3313/customer_db
    username: root
    password: rootPASSWORD

  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka